{
  "name": "rugby-match-tracker",
  "version": "1.0.0",
  "description": "Ficha digital de partido de rugby - Desarrollado con inteligencia artificial",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm ci --silent && npm run build",
    "install-client": "cd client && npm install",
    "postinstall": "cd client && npm ci",
    "vercel-build": "cd client && npm ci --silent && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": ["rugby", "match", "tracker", "sports"],
  "author": "AI Assistant"
}
